// create an array
const arr = [];
const arr2 = [1, 2, 3];
const arr3 = ["1", "2", "3"];
const arr4 = [1, "2", true, []];
//typeof
typeof arr;

//indexing

// push (interactive)
// pop
//shift
// unshift

arr.push(1123); //新增1123到arr尾巴
arr.pop(); //把最後一個值拿掉

arr.unshift("987"); //新增"987"到arr最前面
arr.shift(); //把最前面的值拿掉

// map, filter

//length
console.log(arr.length);
console.log(arr3.length);

//for, foreach個人喜歡用for
